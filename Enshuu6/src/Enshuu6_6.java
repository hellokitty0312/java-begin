
public class Enshuu6_6 {

	public static void main(String[] args) {
		// TODO 自動生成されたメソッド・スタブ
		double [][] a = new double [2][2];

	    MatrixOperation.setMatrix(a); MatrixOperation.showMatrix(a);
	    System.out.println("a^{-1}="); MatrixOperation.showMatrix(MatrixOperation_6.inverse(a));

	    MatrixOperation.setMatrix(a); MatrixOperation.showMatrix(a);
	    System.out.println("a^{-1}="); MatrixOperation.showMatrix(MatrixOperation_6.inverse(a));

	}

}
